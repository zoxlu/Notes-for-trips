# 複製這個檔案成 .env（.env 已經在 .gitignore 裡，不會被 commit）。

# 給網站前端地圖用（要開 Maps JavaScript API + Directions API），
# 並在 Google Cloud Console 設定 HTTP 網域限制（localhost + 正式站網域）。
GOOGLE_MAPS_API_KEY=

# 給「附近公廁」批次查詢腳本用（要開 Places API (New)）。
# 這是伺服器端呼叫，不能沿用上面那把有 HTTP 網域限制的金鑰——
# 請另外建一把，應用程式限制設「無」或「IP 位址」，API 限制只勾 Places API (New)。
# 只有在重新產生 nearby_restrooms 資料時才需要，平常可以留空。
GOOGLE_PLACES_API_KEY=
